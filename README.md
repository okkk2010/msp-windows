# msp-windows

Windows 클라이언트의 기준 프로젝트는 `msp-windows/msp-windows.csproj` 입니다.

- 디버그와 빌드는 이 프로젝트를 기준으로 수행합니다.
- VS Code 설정도 `msp-windows.csproj` 를 기준으로 맞춰져 있습니다.
- 대체 프로젝트 파일은 제거했고, 현재 작업 기준은 `msp-windows.csproj` 하나입니다.
